<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppTheme" parent="android:style/Theme.Black.NoTitleBar" />
	 <style name="Dialog.NoTitle" parent="@android:style/Theme.Dialog">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowBackground">@color/main_color_alpha</item>
    </style>
    
    <style name="ListView.style.Main" parent="@android:style/Widget.ListView">
        <!-- 设置透明背景色 -->
	  <item name="android:cacheColorHint">#00000000</item>
	  <!-- 指定分割线颜色 -->
	  <item name="android:divider">#00000000</item>
	  <!-- 指定分割线高度 -->
	  <item name="android:dividerHeight">15dp</item>
	  <!-- 设置listSelector的颜色在数据的下面 -->
	  <item name="android:drawSelectorOnTop">false</item>
	  <!-- 设置点击选中颜色 -->
	   <item name="android:listSelector">#00000000</item>
	  <!-- 设置ListView默认背景色 -->
	  <item name="android:background">#00000000</item>
	  <!-- 设置ListView距左边距距离 -->
	  
    </style>
</resources>